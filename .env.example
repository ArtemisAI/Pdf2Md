# Docker Compose Environment Configuration for Pdf2Md MCP Server

# =============================================================================
# MCP Server Configuration
# =============================================================================

# Port to expose the MCP server on the host (default: 3000)
MCP_PORT=3000

# CORS origin configuration - set to your client domain(s) for security
# For development: CORS_ORIGIN=*
# For production: CORS_ORIGIN=https://your-domain.com,https://another-domain.com
CORS_ORIGIN=*

# =============================================================================
# File Processing Directories
# =============================================================================

# Directory for temporary file uploads (will be created if it doesn't exist)
UPLOAD_DIR=./uploads

# Directory for processed output files
OUTPUT_DIR=./output

# Shared directory for markdown files (used by get-markdown-file tool)
# Set MD_SHARE_DIR to restrict file access to this directory
MD_SHARE_DIR=./shared

# =============================================================================
# GPU Configuration (Optional - for enhanced audio transcription)
# =============================================================================

# Uncomment the following lines if you have GPU support and want to enable
# GPU-accelerated audio transcription

# CUDA device to use (0 for first GPU, 1 for second, etc.)
# CUDA_VISIBLE_DEVICES=0

# Required for faster-whisper GPU support
# KMP_DUPLICATE_LIB_OK=TRUE

# Optimize thread usage for audio processing
# OMP_NUM_THREADS=4

# =============================================================================
# Redis Configuration (Optional - for session storage)
# =============================================================================

# Redis connection URL if using Redis for session/event storage
# Uncomment if you enable Redis in docker-compose.yml
# REDIS_URL=redis://redis:6379

# =============================================================================
# Advanced Configuration
# =============================================================================

# Python environment configuration
PYTHONUTF8=1

# UV Python package manager path (automatically configured in Docker)
# UV_PATH=/app/.local/bin/uv